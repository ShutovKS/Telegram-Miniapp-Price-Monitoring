class ProductModel {
    constructor({productName, productUrl, currentPrice}) {
        this.productUrl = productUrl;
        this.productName = productName;
        this.currentPrice = currentPrice;
    }
}

export default ProductModel;
