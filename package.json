{
  "scripts": {
    "client:start": "cd src/notify-app-client && npm install && npm start",
    "client:build": "cd src/notify-app-client && npm install && npm run build",
    "client:start-build": "cd src/notify-app-client && npm install && npm run build && serve -s build",

    "server:start": "cd src/notify-app-server && npm install && npm start",
    "server:build": "cd src/notify-app-server && npm install && npm run build",
    "server:start-build": "cd src/notify-app-server && npm install && npm run build && node build/index.js",

    "price-watcher-service:start": "cd src/price-watcher-service && npm install && npm start",
    "price-watcher-service:build": "cd src/price-watcher-service && npm install && npm run build",
    "price-watcher-service:start-build": "cd src/price-watcher-service && npm install && npm run build && node build/index.js",

    "database:start": "cd src/database && docker-compose up -d",
    "database:stop": "cd src/database && docker-compose down"
  }
}